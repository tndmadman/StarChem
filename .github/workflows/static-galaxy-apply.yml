name: Apply Static Galaxy Patch

on:
  push:
    branches:
      - agent/static-galaxy-territory-expansion

permissions:
  contents: write

jobs:
  apply:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/static-galaxy-territory-expansion
          fetch-depth: 0
      - name: Apply reviewed patch
        shell: bash
        run: |
          if [[ ! -f .github/static-galaxy-final.ready ]]; then
            echo "Patch payload is not complete yet."
            exit 0
          fi
          cat .github/static-galaxy-final.patch.gz.b64.part* > /tmp/static-galaxy.patch.gz.b64
          base64 -d /tmp/static-galaxy.patch.gz.b64 | gzip -d > /tmp/static-galaxy.patch
          git apply --check /tmp/static-galaxy.patch
          git apply /tmp/static-galaxy.patch
          rm -f .github/static-galaxy-final.patch.b64.part* .github/static-galaxy-final.patch.gz.b64.part* .github/static-galaxy-final.ready
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "Complete static galaxy territory implementation"
          git push origin HEAD:agent/static-galaxy-territory-expansion
