name: CI

on:
  pull_request:
    branches:
      - main

permissions:
  contents: write

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  diagnose-auth-patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ref: agent/server-scoped-password-verifiers
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
      - uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.10.2'
      - name: Apply asserted patch
        run: |
          python3 tools/repair_auth_patcher.py
          python3 tools/apply_server_scoped_auth_fix.py
      - name: Compile patched source
        id: compile
        shell: bash
        run: |
          set +e
          gradle clean compileJava --no-daemon > compile.log 2>&1
          STATUS=$?
          cat compile.log
          echo "status=$STATUS" >> "$GITHUB_OUTPUT"
          exit 0
      - name: Publish compile diagnostic
        if: steps.compile.outputs.status != '0'
        shell: bash
        run: |
          set -euo pipefail
          cp compile.log compile-errors.txt
          git config user.name "StarChem Security Bot"
          git config user.email "actions@users.noreply.github.com"
          git add -f compile-errors.txt
          git commit -m "Record server-scoped auth compile failure"
          git push origin HEAD:agent/server-scoped-password-verifiers
          exit 1
