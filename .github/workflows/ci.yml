name: CI

on:
  pull_request:
    branches:
      - main

permissions:
  contents: write

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  verify:
    name: Compile, validate, and package
    runs-on: ubuntu-latest
    steps:
      - name: Check out source
        uses: actions/checkout@v4
        with:
          ref: agent/schedule-inactive-systems
          fetch-depth: 1
          show-progress: false

      - name: Apply issue 190 implementation
        shell: bash
        run: |
          python3 - <<'PY'
          from pathlib import Path

          lines = Path('.github/workflows/agent-issue-190.yml').read_text().splitlines()
          start = next(i for i, line in enumerate(lines) if "python3 - <<'PY'" in line)
          end = next(i for i in range(start + 1, len(lines)) if lines[i].strip() == 'PY')
          body = lines[start + 1:end]
          indent = min((len(line) - len(line.lstrip()) for line in body if line.strip()), default=0)
          script = '\n'.join(line[indent:] if line.strip() else '' for line in body)
          Path('/tmp/issue-190-patch.py').write_text(script)
          print('Extracted patch lines:', len(body), 'indent:', indent)
          PY
          python3 /tmp/issue-190-patch.py

      - name: Set up Java 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Set up Gradle 8.10.2
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.10.2'

      - name: Verify focused scheduler behavior
        run: gradle validateSystemSimulationScheduler --no-daemon

      - name: Verify complete project
        run: gradle clean check jar --no-daemon

      - name: Commit verified implementation
        shell: bash
        run: |
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add src/main/java/com/tndmadman/rts/PeerServerSide.java \
                  src/main/java/com/tndmadman/rts/AuthoritativeSystemScheduler.java \
                  src/main/java/com/tndmadman/rts/SystemSimulationScheduler.java \
                  src/main/java/com/tndmadman/rts/SystemSimulationSchedulerValidator.java
          git commit -m "Schedule inactive systems by due time"
          git push origin HEAD:agent/schedule-inactive-systems
