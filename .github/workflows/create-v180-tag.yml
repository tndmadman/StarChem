name: Retired v1.8.0 Release Helper

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Explain retirement
        run: echo 'StarChem v1.8.0 is already published. This temporary helper is retired and has no write permissions.'
