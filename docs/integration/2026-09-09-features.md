# 2026-09-09 Feature Integration Manifest

## Goal
Collapse the feature work from today's agent wave into this single branch without replaying unrelated or stale branch history. This branch started from frozen main SHA `9e9cd90bcdc8bd35707220dc357f121714a8e858`.

Target issues: **#380, #381, #382, #383, #384, #386, #387**.

Do not merge graphics or security work into this branch. Issue #385 belongs to the graphics rollup because its implementation is the shared visual-definition layer.

## Integrated source decisions
- **#380 Persistent fleets — complete.** Integrated from `issue-380-persistent-fleets-finish`. The older `issue-380-persistent-fleets` branch was treated as stale/superseded rather than merged wholesale. Integrated persistent fleet authority/state and its dedicated validator.
- **#381 System control v2 — complete.** Integrated from `issue-381-system-control-v2`. Preserved the strategic control, supply, infrastructure, intel, recovery, production, map snapshot, configuration, and validator changes. Shared `BuildSystem.java` and `Unit.java` behavior was reconciled manually with #382/#386 instead of taking either source file wholesale.
- **#382 Research tech tree — complete.** Integrated feature-owned work from `issue-382-research-tech-tree`. `issue-382-research-tech-tree-refresh` was inspected but not merged wholesale because it had absorbed unrelated later security/main work. Integrated the generalized research policy, unlock/doctrine rules, research system/configuration, validation script, and dedicated research workflow.
- **#383 Procedural/custom galaxy — complete.** Integrated from `issue-383-procedural-galaxy`. Integrated deterministic generation settings/wire/setup/preview/topology/runtime/persistence support and the galaxy-generation validator. The source workflow and TCP validator were not transplanted because those source versions pulled unrelated graphics/security checks. `GalaxyRuntimeOptions.java` was repaired and then semantically merged with #384.
- **#384 Scenario framework — complete.** Integrated from the clean scenario state at commit `ca4daea7f33edfecfd3e2de07835e2ad8082a24c` on `feature/issue-384-scenario-framework`, rather than the contaminated branch head. Integrated the three authored scenarios, scenario rules/director/initializer/launch/metrics/objective bridge, authoritative scheduler integration, save/reconnect projection, failure/completion UI behavior, snapshot/network integration, skirmish compatibility, validator, and clean scenario-only workflow.
- **#386 Formation system v2 — complete.** Integrated from `issue-386-formation-v2`. Preserved deterministic role-aware formation planning, controller/intent, command queue/order integration, cohesion speed caps, and validator. The source `TcpConnectionIdentityValidator.java` security changes were excluded.
- **#387 Production/logistics diagnostics — complete.** Integrated feature-owned work from `issue-387-production-diagnostics`: causal analysis, bounded diagnostic service/UI, production queue integration, wormhole route state support, and the dedicated causal-analysis validator. Source release-script edits were not copied wholesale; the canonical release gate was reconciled once after all feature streams landed.

## Shared-file conflict resolutions
- **#380 + #386 command semantics:** persistent fleet state remains authoritative while #386 formation planning feeds the existing command queue/order model rather than creating a parallel movement path.
- **#381 + #386 `Unit.java`:** effective movement speed retains #381's strategic logistics multiplier and then applies #386's active formation speed cap. #386's speed-cap lifecycle and movement overloads are preserved.
- **#381 + #382 `BuildSystem.java`:** #382 `ResearchPolicy` gates are authoritative for ship/station/research unlock checks, while #381 supply/control cache invalidation is retained when bases are created.
- **#383 + #384 `GalaxyRuntimeOptions.java`:** scenario-selected galaxy copy count is applied during normal configuration without resetting an explicit #383 procedural-generation override. Procedural settings/seed APIs remain intact.
- **Frozen-baseline compile seam:** the frozen baseline already contained a newer challenge/proof `PeerServerSide.resume(...)` signature while `TcpConnectionIdentityValidator` still called the obsolete signature. The integration branch updates only that validator call to perform the current session challenge/proof handshake; no security implementation was imported from a feature source branch.

## Explicitly excluded / superseded source material
- `issue-380-persistent-fleets` — superseded by `issue-380-persistent-fleets-finish`.
- `issue-382-research-tech-tree-refresh` — not merged wholesale; it contained unrelated later security/main history.
- #383 source workflow — excluded because it invoked unrelated graphics validation; #383 feature code and validator are present without that contamination.
- #383/#386 source TCP identity/security variants — excluded as security-stream work.
- `feature/issue-384-scenario-framework` branch head — excluded because later history absorbed unrelated main/security/graphics changes; clean scenario commit `ca4daea7...` was used instead.
- Source release-regression scripts from individual branches — not copied wholesale. `validation/run-release-regressions.sh` was reconciled after integration and now represents all seven feature streams while preserving the frozen baseline checks.

## Validation wiring
The canonical release regression gate now runs:
- `scripts/validate_research.py` for #382
- `Issue380PersistentFleetValidator` for #380
- `SystemControlValidator` for #381
- `Issue383GalaxyGenerationValidator` for #383
- `ScenarioFrameworkValidator` for #384
- `Issue386FormationValidator` for #386
- `Issue387ProductionCausalAnalysisValidator` for #387

The existing #382 research workflow remains in place. The clean #384 `scenario-validation.yml` workflow is included. Existing frozen-baseline validation remains intact.

## Validation notes
- GitHub Actions compilation succeeded after repairing the frozen-baseline `TcpConnectionIdentityValidator` resume-call mismatch.
- Final live workflow conclusions are reported from the branch head in the integration agent's completion report; the manifest itself does not claim a workflow green until GitHub reports it.

## Status
- [x] #380 — integrated
- [x] #381 — integrated
- [x] #382 — integrated
- [x] #383 — integrated
- [x] #384 — integrated
- [x] #386 — integrated
- [x] #387 — integrated

## Completion boundary
This branch is the feature rollup only. It intentionally does **not** merge to `main`, does **not** open a PR to `main`, and does **not** pull source-branch graphics/security implementations into the feature integration history.
