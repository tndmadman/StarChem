# 2026-09-09 Security Integration Manifest

## Goal
Collapse today's security fixes into this single branch, starting from frozen main SHA `9e9cd90bcdc8bd35707220dc357f121714a8e858`, without replaying stale branch history or losing fixes that already landed directly on main.

Target issues: **#395, #397, #400, #402, #404, #406**.

## Source branches
- #395 respawn authority: `fix/issue-395-respawn-authority`.
- #397 observer conversion authorization: `security/issue-397-observer-conversion`.
- #400 proof-gated session resume: `fix/issue-400-session-resume-proof`.
- #402 real remote source identity/rate limiting: `fix/issue-402-auth-source-address`.
- #404 pre-auth handshake hardening: `fix/issue-404-preauth-handshake-hardening`.
- #406 TLS first-use identity verification: `fix/issue-406-tls-first-use`.

## Important current-state rule
Several security PRs already landed on main. Do not assume the old branch head is still the correct merge source. For every issue:
1. Read the issue and its merged/open PR history.
2. Compare the source branch with the frozen current-main baseline.
3. Determine whether the intended fix is already present, partially present, or has unique follow-up work.
4. Reconcile the final intended behavior into this branch without reverting newer main changes.

## Security grouping / order
### Game-state authority
1. #395 respawn authority.
2. #397 observer conversion authorization.

These should share a coherent, server-authoritative concept of global defeat/player state and must not trust UI availability or client-provided active-system state.

### Pre-auth identity and resource limits
3. #402 remote registration/source identity.
4. #404 pre-auth frame/handshake hardening.

Keep the real remote network identity available for rate limiting, moderation, logging, and abuse controls. Keep authorization policy separate from network identity. Bound hostile pre-auth parsing/allocation before expensive work.

### Session / TLS identity
5. #400 session resume proof enforcement.
6. #406 TLS first-use trust hardening.

Ensure challenge/nonce/proof behavior is actually enforced on live paths and that first-use server identity is not silently treated as authenticated when no trusted fingerprint is known.

## Integration rules
- Server authority wins over client/UI state.
- Never reintroduce bearer-only session resume when proof support is advertised.
- Never substitute fake loopback/source addresses for remote clients in security decisions.
- Reject oversized/invalid pre-auth data before expensive parsing/allocation.
- Do not silently replace an existing pinned TLS identity.
- Preserve compatibility only where it does not weaken the intended security fix.
- Do not pull unrelated graphics/feature files from stale branch ancestry.
- If two issue fixes touch the same method/protocol state machine, reconcile them as one coherent implementation rather than accepting last-write-wins merge behavior.
- Keep this manifest updated with issue-by-issue integration status and any intentional deviations from the original issue recommendation.

## Required regression focus
- #395: active player rejected; globally defeated player gets exactly one authoritative respawn; viewed system cannot choose spawn destination; duplicate/concurrent requests cannot duplicate forces.
- #397: active player cannot self-convert; invitation/policy checks cannot be bypassed; observer visibility does not leak unauthorized FOW perspective.
- #400: missing/invalid/replayed/stale proof rejected; valid single-use proof succeeds; session material rotates as designed.
- #402: unrelated remote sources have independent abuse/rate-limit identity; moderation/logging retain the real address; remote registration policy still works.
- #404: oversized/delimiter-heavy pre-auth frames rejected before disproportionate parsing/allocation; legitimate handshake still succeeds; authenticated frame limits remain separate.
- #406: preconfigured matching fingerprint succeeds; mismatch fails; existing pin cannot silently change; first-seen identity is explicitly unverified until approved/preseeded according to final design.

## Completion gate
Before declaring this branch complete:
- All 6 issues have an explicit status below.
- Current main behavior has been reviewed so already-landed security fixes are not accidentally reverted.
- All security/auth/network/game-authority validators pass together from this branch head.
- Multiplayer join/register/resume/reconnect flows still function for legitimate clients.
- No security-sensitive changes are stranded only on an obsolete source branch.
- No unrelated graphics/feature implementation is present in the final diff.
- Report the final branch head SHA and issue-by-issue status.

## Status
- [ ] #395
- [ ] #397
- [ ] #400
- [ ] #402
- [ ] #404
- [ ] #406
