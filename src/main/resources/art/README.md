# Art resource root

Repository-backed graphics assets live here and must be loaded through `ArtAssetCache` so they resolve from both development classpaths and packaged JARs.

See `docs/ART_ASSETS.md` for supported formats, naming, alpha, size/memory guidance, failure behavior, validation commands, and the category layout.
